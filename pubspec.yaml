name: query_wizard
description: Preparation tool of the query text.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  animations: ^2.0.0
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  flutter_bloc: ^7.0.0
  flutter_hooks: ^0.17.0
  flutter_localized_locales: ^2.0.1
  equatable: ^2.0.0
  intl: ^0.17.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  bloc_test: ^8.0.0
  mockito: ^5.0.8

flutter:

  generate: true
  uses-material-design: true
